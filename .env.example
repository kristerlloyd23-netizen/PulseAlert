# Copy this file to ".env" and fill in real values.

# Generate these once with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=

# Optional: change the JWT signing secret from the demo default
JWT_SECRET=capstone-demo-secret-change-me

# Optional: change the port the server runs on
PORT=3000
